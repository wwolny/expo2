import React, { Component } from 'react';
import { View, Text } from 'react-native';

class MyAccScreen extends Component {
  render() {
    return (
      <Text>
        MyAccScreen
      </Text>
    );
  }
}

export default MyAccScreen;
